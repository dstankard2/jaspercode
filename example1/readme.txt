
An application for testing JasperCode.

This file shouldn't be in the application output directory

To run example, run JasperCode (runOnce mode) and then do the following
mvn package
java -jar target\example1-jar-with-dependencies.jar

Then open your web browser to URL: http://localhost:8080/index.html